#!/bin/sh
set -e

# Deploy to Firebase Hosting at https://mandelbrot-rust.web.app/
if [ "$CIRCLECI" = "true" ]; then
  firebase deploy --token "$FIREBASE_TOKEN" --non-interactive
else
  firebase deploy
fi
